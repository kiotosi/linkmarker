{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-server/**", "dist-client/**"]
    },
    "test": {
      "outputs": [],
      "outputMode": "full"
    },
    "lint": {
      "outputs": [],
      "outputMode": "full"
    },
    "dev": {
      "cache": false
    }
  }
}
