/** @type {import('jest').Config} */
const config = {
  verbose: true,
  moduleFileExtensions: ['js', 'ts', 'node', 'vue', 'json'],
  passWithNoTests: true,
  automock: false,
};

module.exports = config;
